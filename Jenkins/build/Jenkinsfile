// See: https://github.com/openshift/jenkins-plugin
node {
  stage('Build') {
    // Triggers the Fluentd Openshift BuildConfig
    openshiftBuild(buildConfig: 'hogarama-jenkins-build', showBuildLogs: true);
 }
}
